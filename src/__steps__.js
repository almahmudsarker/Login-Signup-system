/**
 * CONTEXT API
 * 1. Create a context (exported from a file)
 * 2.create provider
 * 3.set context value
 * 4.to access the context value, use useContext hook
 */

/**
 * 1. main.jsx set auth provider
 * 2.inside provider, access children props & set user
 */

/**
 * 1.break down the create user function
 * 2.auth provider will have auth and pass email and password from register
 */

/**
 * 1.Private route
 */
